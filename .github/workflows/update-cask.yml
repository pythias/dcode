name: Update Homebrew Cask

# release.yml 已在发版后推送 Cask；此处仅用于手动对齐 tap（例如修复文案）。
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release 版本号（不带 v），例如 0.2.0"
        required: true

jobs:
  update-cask:
    runs-on: macos-latest

    steps:
      - name: Resolve release tag
        id: release
        run: |
          VERSION="${{ inputs.version }}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Download DMGs and compute sha256
        id: sha
        run: |
          VERSION="${{ steps.release.outputs.version }}"
          TAG="${{ steps.release.outputs.tag }}"
          BASE="https://github.com/pythias/dcode/releases/download/${TAG}"
          curl -fL -o /tmp/dcode_aarch64.dmg "${BASE}/DCode_${VERSION}_aarch64.dmg"
          curl -fL -o /tmp/dcode_x64.dmg "${BASE}/DCode_${VERSION}_x64.dmg"
          echo "sha256_arm=$(shasum -a 256 /tmp/dcode_aarch64.dmg | awk '{print $1}')" >> "$GITHUB_OUTPUT"
          echo "sha256_intel=$(shasum -a 256 /tmp/dcode_x64.dmg | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - name: Checkout homebrew-tools tap
        uses: actions/checkout@v4
        with:
          repository: pythias/homebrew-tools
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tools

      - name: Update Cask
        env:
          PKG_VERSION: ${{ steps.release.outputs.version }}
          SHA_ARM: ${{ steps.sha.outputs.sha256_arm }}
          SHA_INTEL: ${{ steps.sha.outputs.sha256_intel }}
        run: |
          cd homebrew-tools
          mkdir -p Casks
          cat <<'RUBY' > Casks/dcode.rb
          cask "dcode" do
            version "__PKG_VERSION__"

            on_arm do
              url "https://github.com/pythias/dcode/releases/download/v#{version}/DCode_#{version}_aarch64.dmg",
                  verified: "github.com/pythias/dcode/"
              sha256 "__SHA_ARM__"
            end

            on_intel do
              url "https://github.com/pythias/dcode/releases/download/v#{version}/DCode_#{version}_x64.dmg",
                  verified: "github.com/pythias/dcode/"
              sha256 "__SHA_INTEL__"
            end

            name "DCode"
            desc "A minimal IDE built with Tauri + React"
            homepage "https://github.com/pythias/dcode"

            app "DCode.app"
          end
          RUBY
          sed -i '' \
            -e "s/__PKG_VERSION__/${PKG_VERSION}/g" \
            -e "s/__SHA_ARM__/${SHA_ARM}/g" \
            -e "s/__SHA_INTEL__/${SHA_INTEL}/g" \
            Casks/dcode.rb
          ruby -c Casks/dcode.rb
          cat Casks/dcode.rb

      - name: Commit and push
        run: |
          cd homebrew-tools
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/dcode.rb
          if git diff --cached --quiet; then
            echo "Casks/dcode.rb already matches."
            exit 0
          fi
          git commit -m "chore: update dcode to ${{ steps.release.outputs.version }}"
          git push
